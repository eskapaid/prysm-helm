apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "prysm-beacon-node.fullname" . }}-slasher
  labels:
    app: {{ template "prysm-beacon-node.name" . }}-slasher
    {{- include "prysm-beacon-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": keep
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: {{ .Values.slasher.storage.capacity }}
  hostPath:
    path: /data